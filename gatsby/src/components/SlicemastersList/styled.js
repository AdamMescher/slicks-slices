import styled from 'styled-components';

const StyledSlicemasters = styled.div`
  display: grid;
  grid-gap: 2rem;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
`;

export default StyledSlicemasters;
